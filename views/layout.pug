doctype html
html(lang='fr')
  head
    title #{title}
    link(rel='shortcut icon', href='/images/logo.png')
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    meta(name='author', content='Manuel ADELE')
    meta(name='description', content='social network fullstack javascript node expressjs')
    link(rel='stylesheet', href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css')
    link(rel='stylesheet', href='/stylesheets/style.css')
    link(rel='stylesheet' href='/stylesheets/bootstrap-social-gh-pages/bootstrap-social.css')
    link(rel='stylesheet' href='/font-awesome-4.7.0/css/font-awesome.min.css')
    link(rel='stylesheet', href='https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.25.2/codemirror.min.css')
    script(type="text/javascript" src='https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.25.2/codemirror.min.js')    
    link(rel='stylesheet', href='https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.25.2/theme/base16-dark.min.css')
    link(rel='stylesheet', href='https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.25.2/theme/dracula.min.css')
    script(type="text/javascript" src="https://maps.googleapis.com/maps/api/js?key=AIzaSyBA3aPQ0M3lmwom2xMr44K8AnPOug84ENI")
    script(type="text/javascript" src='/javascripts/google-maps.js')
    
    
  body
    <!--facebook SDK -->
    script.
        function statusChangeCallback(response) {
        if (response.status === 'connected') {
        testAPI();
        } else {
        document.getElementById('status').innerHTML = 'Please log' + 'into this app.';
        };
        };
        function checkLoginState() {
        FB.getLoginStatus(function(response){
        statusChangeCallback(response)
        });
        
        };
        window.fbAsyncInit = function() {
        FB.init({
        appId: '288860251564912',
        cookie: true,
        xfbml: true,
        version: 'v2.8'
        });
        FB.getLoginStatus(function(response){
        statusChangeCallback(response);
        console.log(response);
        });
        };
        (function(d,s, id){
        var js, fjs = d.getElementsByTagName(s)[0];
        if(d.getElementById(id)) return;
        js = d.createElement(s); js.id = id;
        js.src = "//connect.facebook.net/en_US/sdk.js";
        fjs.parentNode.insertBefore(js, fjs);
        }(document, 'script', 'facebook-jssdk'));
        function testAPI() {
        console.log('Welcome!  Fetching your information.... ');
        FB.api('/me', function(response) {
        console.log('Successful login for: ' + response.name);
        console.log(response);
        });
        };
    script.
        window.fbAsyncInit = function() {
        FB.init({
        appId      : '288860251564912',
        xfbml      : true,
        version    : 'v2.9'
        });
        FB.AppEvents.logPageView();
        };
        (function(d, s, id){
        var js, fjs = d.getElementsByTagName(s)[0];
        if (d.getElementById(id)) {return;}
        js = d.createElement(s); js.id = id;
        js.src = "//connect.facebook.net/en_US/sdk.js";
        fjs.parentNode.insertBefore(js, fjs);
        }(document, 'script', 'facebook-jssdk'));
    include partials/header

    .container
      include partials/flash
      block content

    include partials/footer

    <!-- Latest compiled and minified Jquery -->
    script(src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.0/jquery.min.js")
    <!-- Latest compiled and minified Javascript -->
    script(src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js")
    <!-- Custom animated TEXT -->    
    script(type="text/javascript" src='/javascripts/lib/textillate-master/assets/jquery.fittext.js')    
    script(type="text/javascript" src='/javascripts/lib/textillate-master/assets/jquery.lettering.js')    
    script(type="text/javascript" src='/javascripts/lib/textillate-master/jquery.textillate.js')
    script(type="text/javascript" src='/javascripts/flash.js')
    <!-- editor code Javascript -->
    script(type="text/javascript" src='https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.25.2/mode/javascript/javascript.min.js')
    script.
      var editor = CodeMirror.fromTextArea(document.getElementById("code-editor"),{
      lineNumbers: true,
      theme: "dracula"
      });
    <!-- Api Google Maps -->      
    script.
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
      ga('create', '933750438128-16e0pji53v370odgsbru8di4viuttv5j.apps.googleusercontent.com', 'auto');
      ga('send', 'pageview');
    <!-- socket.io client -->
    script(src='/socket.io/socket.io.js')
    script.
      var socket = io.connect('http://localhost:3000');
    script(type="text/javascript" src="/javascripts/main.js")

    

    
